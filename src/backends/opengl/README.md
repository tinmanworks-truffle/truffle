# OpenGL Backend Placeholder

This directory owns the future OpenGL backend implementation. The
`TRUFFLE_BUILD_BACKEND_OPENGL` option reserves its build boundary until the
backend milestone adds dependency discovery, targets, and implementation code.
