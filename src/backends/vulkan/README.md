# Vulkan Backend Placeholder

This directory owns the future Vulkan backend implementation. The
`TRUFFLE_BUILD_BACKEND_VULKAN` option reserves its build boundary until the
backend milestone adds SDK discovery, targets, and implementation code.
